version: '3.7'

services:
  traefik:
    image: traefik:latest
    ports:
      - "8090:80"
      - "8091:8080"
    volumes:
      - ./:/etc/traefik/
  nginx-1:
    image: nginx:latest
    volumes:
      - ./nginx-1:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
  nginx-2:
    image: nginx:latest
    volumes:
      - ./nginx-2:/usr/share/nginx/html:ro
    ports:
      - "8081:80"
